#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: 1st integer
 * @b: 2nd integer
 *
 * Return: result (product of a and b)
 */

int mul(int a, int b)
{
	int a, b;
	int result;

	result = (a * b);

	return (result);
}
