#include "main.h"

/**
 * print_line - prints a straight line
 * @n: number of times the character
 */

void print_line(int n)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		if (n != 0)
			_putchar(98);
	}
}

